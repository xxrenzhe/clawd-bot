---
const currentYear = new Date().getFullYear();

// Trusted domains that should pass link juice (no nofollow)
const trustedDomains = new Set([
  'github.com',
  'discord.gg',
  'twitter.com',
  'anthropic.com',
]);

function getDomain(url: string): string | null {
  try {
    const parsed = new URL(url);
    return parsed.hostname.replace(/^www\./, '');
  } catch {
    return null;
  }
}

function getRelAttribute(href: string): string | undefined {
  if (!href.startsWith('http')) return undefined;

  const domain = getDomain(href);
  if (!domain) return 'noopener noreferrer';

  // Check if domain is trusted
  const domainParts = domain.split('.');
  const checkDomains = [domain];
  if (domainParts.length > 2) {
    checkDomains.push(domainParts.slice(-2).join('.'));
  }

  for (const d of checkDomains) {
    if (trustedDomains.has(d)) {
      return 'noopener'; // Trusted: pass link juice
    }
  }

  return 'noopener noreferrer';
}

const referralUrl = import.meta.env.HOSTING_REFERRAL_URL || 'https://hosting.com';
const trackingId = import.meta.env.HOSTING_REFERRAL_TRACKING_ID || 'clawdbot';

function buildHostingUrl(context: string) {
  const url = new URL(referralUrl);
  url.searchParams.set('utm_source', 'clawd-bot.app');
  url.searchParams.set('utm_medium', 'referral');
  url.searchParams.set('utm_campaign', trackingId);
  url.searchParams.set('utm_content', `footer-${context}`);
  return url.toString();
}

const footerLinks = {
  Product: [
    { name: 'Features', href: '/features' },
    { name: 'Hosting', href: '/hosting', highlight: true },
    { name: 'Documentation', href: 'https://github.com/anthropics/anthropic-quickstarts' },
    { name: 'Tutorials', href: '/articles' },
  ],
  Resources: [
    { name: 'Blog', href: '/articles' },
    { name: 'Deploy Guide', href: '/hosting' },
    { name: 'GitHub', href: 'https://github.com/anthropics/anthropic-quickstarts' },
    { name: 'Community', href: 'https://discord.gg/anthropic' },
  ],
  Company: [
    { name: 'About Us', href: '/about' },
    { name: 'Contact', href: '/contact' },
    { name: 'Privacy Policy', href: '/privacy' },
    { name: 'Terms of Service', href: '/terms' },
  ],
};
---

<footer class="border-t border-gray-200 bg-gray-50 dark:border-gray-800 dark:bg-gray-900">
  <!-- Hosting Promo Banner -->
  <div class="bg-gradient-to-r from-blue-600 to-indigo-600 px-4 py-4 text-white sm:px-6 lg:px-8">
    <div class="mx-auto flex max-w-7xl flex-col items-center justify-between gap-3 sm:flex-row">
      <div class="flex items-center gap-3">
        <div class="hidden rounded-full bg-white/20 p-2 sm:block">
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"/>
          </svg>
        </div>
        <p class="text-center text-sm font-medium sm:text-left">
          <span class="font-bold">Run Clawdbot 24/7</span> — Get 30% off VPS hosting for your AI assistant
        </p>
      </div>
      <a
        href={buildHostingUrl('banner')}
        target="_blank"
        rel="noopener sponsored"
        class="inline-flex items-center gap-2 whitespace-nowrap rounded-lg bg-white px-4 py-2 text-sm font-bold text-blue-600 shadow-sm transition-all hover:bg-gray-100 hover:shadow-md"
        data-analytics-event="hosting-cta-click"
        data-analytics-context="footer-banner"
      >
        Claim Discount
        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
        </svg>
      </a>
    </div>
  </div>

  <div class="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 gap-8 md:grid-cols-4">
      <!-- Brand -->
      <div class="col-span-1">
        <a href="/" class="flex items-center gap-2 text-xl font-bold text-gray-900 dark:text-white">
          <svg class="h-8 w-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
          </svg>
          <span>Clawdbot</span>
        </a>
        <p class="mt-4 text-sm text-gray-600 dark:text-gray-400">
          Your open-source AI assistant for productivity and automation.
        </p>
      </div>

      <!-- Links -->
      {Object.entries(footerLinks).map(([category, links]) => (
        <div>
          <h3 class="text-sm font-semibold uppercase tracking-wider text-gray-900 dark:text-white">
            {category}
          </h3>
          <ul class="mt-4 space-y-3">
            {links.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-sm text-gray-600 transition-colors hover:text-blue-600 dark:text-gray-400 dark:hover:text-blue-400"
                  target={link.href.startsWith('http') ? '_blank' : undefined}
                  rel={getRelAttribute(link.href)}
                  title={link.href.startsWith('http') ? `Visit ${link.name}` : undefined}
                >
                  {link.name}
                  {'highlight' in link && link.highlight && (
                    <span class="ml-1 inline-flex items-center rounded bg-orange-100 px-1.5 py-0.5 text-xs font-medium text-orange-800 dark:bg-orange-900 dark:text-orange-200">
                      New
                    </span>
                  )}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>

    <!-- Bottom Bar -->
    <div class="mt-12 border-t border-gray-200 pt-8 dark:border-gray-800">
      <div class="flex flex-col items-center justify-between gap-4 md:flex-row">
        <p class="text-sm text-gray-600 dark:text-gray-400">
          © {currentYear} Clawdbot. All rights reserved.
        </p>
        <div class="flex gap-6">
          <a
            href="https://github.com/anthropics/anthropic-quickstarts"
            target="_blank"
            rel="noopener"
            class="text-gray-600 transition-colors hover:text-blue-600 dark:text-gray-400 dark:hover:text-blue-400"
            aria-label="GitHub"
            title="Visit our GitHub repository"
          >
            <svg class="h-6 w-6" fill="currentColor" viewBox="0 0 24 24">
              <path fill-rule="evenodd" d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z" clip-rule="evenodd"></path>
            </svg>
          </a>
          <a
            href="https://twitter.com/anthropicai"
            target="_blank"
            rel="noopener"
            class="text-gray-600 transition-colors hover:text-blue-600 dark:text-gray-400 dark:hover:text-blue-400"
            aria-label="Twitter"
            title="Follow us on Twitter"
          >
            <svg class="h-6 w-6" fill="currentColor" viewBox="0 0 24 24">
              <path d="M8.29 20.251c7.547 0 11.675-6.253 11.675-11.675 0-.178 0-.355-.012-.53A8.348 8.348 0 0022 5.92a8.19 8.19 0 01-2.357.646 4.118 4.118 0 001.804-2.27 8.224 8.224 0 01-2.605.996 4.107 4.107 0 00-6.993 3.743 11.65 11.65 0 01-8.457-4.287 4.106 4.106 0 001.27 5.477A4.072 4.072 0 012.8 9.713v.052a4.105 4.105 0 003.292 4.022 4.095 4.095 0 01-1.853.07 4.108 4.108 0 003.834 2.85A8.233 8.233 0 012 18.407a11.616 11.616 0 006.29 1.84"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</footer>
