---
title: "Moltbot Code Review: AI-Powered PR Analysis [2026]"
description: "Use Moltbot for AI-powered code review. Automated PR comments, security scanning, style checks, and customizable review workflows."
pubDate: 2026-01-27
modifiedDate: 2026-01-29
category: "Guide"
tags: ["moltbot","clawdbot","code review","quality","github","pull requests"]
keywords: ["moltbot code review","ai code review","automated pr review","moltbot github","code quality"]
readingTime: 8
featured: false
author: "Moltbot Team"
image: "/images/articles/clawdbot-code-review.jpg"
imageAlt: "Moltbot Code Review"
articleType: "TechArticle"
difficulty: "intermediate"
sources:
  - "https://docs.molt.bot/"
  - "https://github.com/moltbot/moltbot"
relatedArticles:
  - "clawdbot-github-integration"
  - "clawdbot-coding-assistant"
  - "clawdbot-devops-workflows"
---
import HostingCTA from '../../components/CTA/HostingCTA.astro';

# Moltbot Code Review: AI-Powered PR Analysis

![Moltbot Code Review](/images/articles/clawdbot-code-review.jpg)

## Review Capabilities

| Feature | Description |
|---------|-------------|
| PR Summary | Auto-generate PR descriptions |
| Security Scan | Detect vulnerabilities |
| Style Check | Enforce coding standards |
| Complexity | Flag complex functions |
| Test Coverage | Suggest missing tests |

<HostingCTA context="setup" />

## Enable Code Review Skill

```bash
moltbot skills enable code-review
```

Configure in `moltbot.json`:

```json
{
  "skills": {
    "code-review": {
      "enabled": true,
      "languages": ["javascript", "typescript", "python", "go"],
      "checks": ["security", "style", "complexity", "tests"]
    }
  }
}
```

<HostingCTA context="inline" />

## Review a Pull Request

### Via Chat

```
You: Review PR #123 in myorg/myrepo

Moltbot: 📋 PR #123: Add user authentication

Summary:
This PR implements JWT-based authentication with refresh tokens.

✅ Strengths:
- Proper token validation
- Secure password hashing with bcrypt
- Rate limiting on login endpoint

⚠️ Concerns:
1. src/auth.js:45 - Token expiry too long (7 days)
   Recommendation: Use 15-minute access tokens with refresh

2. src/middleware.js:12 - Missing error handling
   The JWT verification could throw unhandled exceptions

3. Missing tests for token refresh flow

Security: No critical issues found
Complexity: 2 functions exceed threshold (score > 15)
```

### Via GitHub Integration

```bash
# Configure GitHub integration
moltbot configure --section skills.code-review.github
```

```json
{
  "skills": {
    "code-review": {
      "github": {
        "autoReview": true,
        "triggerOn": ["opened", "synchronize"],
        "postComments": true
      }
    }
  }
}
```

## Review Configuration

### Custom Rules

```json
{
  "skills": {
    "code-review": {
      "rules": {
        "maxFunctionLength": 50,
        "maxComplexity": 10,
        "requireTests": true,
        "securityPatterns": [
          "eval\\(",
          "innerHTML",
          "dangerouslySetInnerHTML"
        ]
      }
    }
  }
}
```

### Language-Specific Rules

```json
{
  "skills": {
    "code-review": {
      "languages": {
        "javascript": {
          "linter": "eslint",
          "config": ".eslintrc.js"
        },
        "python": {
          "linter": "flake8",
          "config": ".flake8"
        },
        "go": {
          "linter": "golangci-lint",
          "config": ".golangci.yml"
        }
      }
    }
  }
}
```

## Review Workflow

### Manual Review Request

```
You: Review this code for security issues:

\`\`\`javascript
app.get('/user/:id', (req, res) => {
  const query = `SELECT * FROM users WHERE id = ${req.params.id}`;
  db.query(query, (err, result) => {
    res.json(result);
  });
});
\`\`\`

Moltbot: 🚨 Critical Security Issue: SQL Injection

The code directly interpolates user input into a SQL query,
allowing attackers to execute arbitrary SQL.

Vulnerable line:
\`const query = \`SELECT * FROM users WHERE id = ${req.params.id}\`;\`

Fix:
\`\`\`javascript
app.get('/user/:id', (req, res) => {
  const query = 'SELECT * FROM users WHERE id = ?';
  db.query(query, [req.params.id], (err, result) => {
    res.json(result);
  });
});
\`\`\`

Always use parameterized queries to prevent SQL injection.
```

### Batch File Review

```
You: Review all changed files in the last commit

Moltbot: 📊 Reviewing 5 files from commit abc123...

src/api/users.js
├─ ⚠️ Line 23: Unhandled promise rejection
├─ ⚠️ Line 45: Console.log in production code
└─ ✅ No security issues

src/utils/validate.js
├─ ✅ Clean code
└─ 💡 Consider extracting regex patterns to constants

src/models/user.js
├─ ⚠️ Line 12: Password stored without hashing
└─ 🚨 Critical: Fix before merging

tests/user.test.js
└─ ✅ Good test coverage

package.json
└─ ⚠️ 2 dependencies with known vulnerabilities
   Run: npm audit fix
```

## Automated PR Comments

### GitHub Action Integration

```yaml
# .github/workflows/moltbot-review.yml
name: Moltbot Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Moltbot Review
        run: |
          curl -X POST https://your-moltbot.com/api/review \
            -H "Authorization: Bearer ${{ secrets.MOLTBOT_API_KEY }}" \
            -d '{
              "repo": "${{ github.repository }}",
              "pr": ${{ github.event.pull_request.number }},
              "postComments": true
            }'
```

### Comment Format

```markdown
## 🤖 Moltbot Code Review

### Summary
This PR adds user authentication with JWT tokens.

### Issues Found

| Severity | File | Line | Issue |
|----------|------|------|-------|
| 🚨 High | auth.js | 45 | SQL injection vulnerability |
| ⚠️ Medium | api.js | 23 | Missing error handling |
| 💡 Low | utils.js | 12 | Consider refactoring |

### Suggestions
- Add input validation for email format
- Consider adding rate limiting
- Missing unit tests for error cases

---
*Reviewed by [Moltbot](https://molt.bot)*
```

## Review Templates

### Security-Focused Review

```
You: Security review PR #456

Moltbot: 🔒 Security Review for PR #456

Authentication:
├─ ✅ JWT validation present
├─ ⚠️ Token not invalidated on logout
└─ ⚠️ No rate limiting on auth endpoints

Input Validation:
├─ ✅ Email validation
├─ 🚨 Missing sanitization on username
└─ ⚠️ No file type validation on uploads

Dependencies:
├─ 🚨 lodash@4.17.15 - Prototype pollution (CVE-2020-8203)
└─ ⚠️ express@4.17.1 - Update recommended

Recommendations:
1. Add rate limiting: express-rate-limit
2. Sanitize inputs: validator.js or DOMPurify
3. Update vulnerable dependencies
```

### Performance Review

```
You: Review this code for performance

Moltbot: ⚡ Performance Analysis

Issues:
1. N+1 Query Problem (users.js:34)
   Loading related data in a loop causes 100+ queries
   Fix: Use eager loading with JOIN

2. Missing Index (users.js:56)
   Query on 'email' field without index
   Fix: ALTER TABLE users ADD INDEX idx_email (email)

3. Synchronous File Operations (upload.js:12)
   Using fs.readFileSync blocks event loop
   Fix: Use fs.promises.readFile

4. Memory Leak (cache.js:89)
   Cache grows unbounded
   Fix: Add maxSize and TTL to cache

Estimated Impact:
- Query optimization: 80% faster list queries
- Async file ops: 40% better throughput
```

## Troubleshooting

| Issue | Solution |
|-------|----------|
| Review times out | Reduce file count or split PR |
| Missing context | Include related files in review |
| False positives | Adjust rules in config |
| No GitHub comments | Check webhook configuration |

### Debug Mode

```bash
# Verbose review output
moltbot review --verbose --pr myorg/repo#123

# Test configuration
moltbot skills test code-review
```

## Next Steps

- [GitHub integration](/articles/clawdbot-github-integration)
- [Coding assistant](/articles/clawdbot-coding-assistant)
- [DevOps workflows](/articles/clawdbot-devops-workflows)

<HostingCTA context="conclusion" />
